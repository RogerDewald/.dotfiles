# Loaded when no host file matches this machine.
#
# Deliberately almost empty: a machine I have not set up yet should get a
# plain working shell, not another machine's aliases. To claim a machine,
# copy this file to <hostname>.zsh (see `hostname -s`) or write that name
# into ~/.config/zsh/host.
